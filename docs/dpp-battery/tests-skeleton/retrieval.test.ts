// TDD RED PHASE — F-08 Retrieval. Cible : src/dpp/retrieval.ts (cosineSimilarity)
import { describe, it, expect } from 'vitest';

describe('F-08: Retrieval sémantique', () => {
  describe('AC-01: cosineSimilarity top-k sur l index', () => {
    it('retourne les chunks les plus proches, ordonnés', () => {
      expect(true).toBe(false); // RED
    });
  });
  describe('AC-03: confiance dérivée de similarité + tier', () => {
    it('calcule un niveau de confiance cohérent', () => {
      expect(true).toBe(false); // RED
    });
  });
});
